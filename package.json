{
  "name": "3cx-call-log-analyzer",
  "version": "1.0.0",
  "description": "3CX Call Log Analyzer — Parse call logs to extract critical metrics",
  "private": true,
  "scripts": {
    "build": "node -e \"const fs = require('fs'); if (!fs.existsSync('dist')) fs.mkdirSync('dist'); fs.copyFileSync('index.html', 'dist/index.html');\""
  }
}
